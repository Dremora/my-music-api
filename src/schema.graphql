### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Album {
  artist: String!
  comments: String
  firstPlayed: FirstPlayed
  id: UUID!
  sources: [Source!]!
  title: String!
  year: Int
}

input AlbumFilterInput {
  query: String
  year: Int
}

type AlbumPerYearCount {
  count: Int!
  year: Int!
}

union FirstPlayed = FirstPlayedDate | FirstPlayedTime

type FirstPlayedDate {
  day: Int
  month: Int
  year: Int!
}

input FirstPlayedInput {
  day: Int
  month: Int
  timestamp: Int
  year: Int
}

type FirstPlayedTime {
  timestamp: Int!
}

enum Format {
  APE
  FLAC
  MIXED
  MP3
  MPC
  TAK
  WMA
}

enum Location {
  APPLE_MUSIC
  FOOBAR2000
  GOOGLE_MUSIC
  SPOTIFY
}

type Mutation {
  createAlbum(artist: String!, comments: String, firstPlayed: FirstPlayedInput, sources: [NewSourceInput!]!, title: String!, year: Int): Album!
  login(password: String!): Boolean!
}

input NewSourceInput {
  accurateRip: String
  comments: String
  cueIssues: String
  discs: Int
  download: String
  edition: String
  format: Format
  location: Location!
  mbid: UUID
  tagIssues: String
}

type Query {
  album(id: UUID!): Album!
  albumPerFirstPlayedYearCount: [AlbumPerYearCount!]!
  albumPerYearCount: [AlbumPerYearCount!]!
  albums(filter: AlbumFilterInput!): [Album!]!
}

type Source {
  accurateRip: String
  comments: String
  cueIssues: String
  discs: Int
  download: String
  edition: String
  format: Format
  id: ID!
  location: Location!
  mbid: UUID
  tagIssues: String
}

"""
A field whose value is a generic Universally Unique Identifier: https://en.wikipedia.org/wiki/Universally_unique_identifier.
"""
scalar UUID
