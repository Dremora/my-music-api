### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Album {
  artist: String!
  comments: String
  firstPlayed: FirstPlayed
  id: ID!
  sources: [Source!]!
  title: String!
  year: Int
}

input AlbumFilterInput {
  query: String
  year: Int
}

type AlbumPerYearCount {
  count: Int!
  year: Int!
}

union FirstPlayed = FirstPlayedDate | FirstPlayedTime

type FirstPlayedDate {
  day: Int
  month: Int
  year: Int!
}

type FirstPlayedTime {
  timestamp: Int!
}

enum Format {
  APE
  FLAC
  MIXED
  MP3
  MPC
  TAK
  WMA
}

enum Location {
  APPLE_MUSIC
  FOOBAR2000
  GOOGLE_MUSIC
  SPOTIFY
}

type Mutation {
  login(password: String!): Boolean!
}

type Query {
  album(id: ID!): Album!
  albumPerFirstPlayedYearCount: [AlbumPerYearCount!]!
  albumPerYearCount: [AlbumPerYearCount!]!
  albums(filter: AlbumFilterInput!): [Album!]!
}

type Source {
  accurateRip: String
  comments: String
  cueIssues: String
  discs: Int
  download: String
  edition: String
  format: Format
  id: ID!
  location: Location!
  mbid: String
  tagIssues: String
}
